# Source: rag/charts/minio/templates/route.yaml
kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: minio-webui
spec:
  to:
    kind: Service
    name: rag-minio
    weight: 100
  port:
    targetPort: webui
  wildcardPolicy: None
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
